// Файл: src/routes/currency.routes.js
import { Router } from 'express';

const currencyRouter = Router();

// Здесь вы позже сможете добавить обработчики для маршрутов, связанных с валютой
// Например:
// currencyRouter.get('/', getCurrencyController);

export default currencyRouter;